# Synthetic AxeOS Fixtures

These fixtures model the documented public fields from
`GET /api/system/info` in ESP-Miner/AxeOS firmware. Every value is synthetic.
The MAC address is locally administered and does not identify real hardware.
No fixture contains a household address, hostname, SSID, wallet, pool URL, or
credential.
